#include "monty.h"
/**
 * f_stack - sets the format of the data to a stack
 * @head: the pointer to stack's head
 * @counter: counts the lines executed
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	var.lifi = 0;
}
